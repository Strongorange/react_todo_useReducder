// import React from "react";

// const List = ({ name, children }) => {
//   return (
//     <>
//       <h1>{name}</h1>
//       <ul>{children}</ul>
//     </>
//   );
// };

// export default List;

import React from "react";
export default ({ name, children }) => (
  <>
    <h1>{name}</h1>
    <ul>{children}</ul>
  </>
);
